### Exercise 14.1.3
Write a nonrecursive version of OS-SELECT
***
### `Answer`
    rbt* SelectByRank(rbt* headList, int targetNode){
        bool found = false;
        int rankNow;
        while(found != true){
            if(rankNow > t
    }
